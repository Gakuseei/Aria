# PII Exposure Scan Report

**Scanned**: 2026-03-16
**Repository**: AriaApp (Electron + React + Vite)
**Architecture**: 100% local-only (Ollama), no cloud APIs

---

## Findings

### 1. SECURITY.md — Developer Contact Email

- **file**: SECURITY.md
- **line**: 37
- **category**: hardcoded-pii
- **severity**: low
- **detail**: Developer email `erikschaefer07@icloud.com` is embedded as a security contact. This is an intentional, public-facing disclosure for responsible vulnerability reporting.
- **recommendation**: No action needed. This is standard practice for open-source security policies. The email is the maintainer's public contact, not user PII.

### 2. package-lock.json — Third-Party Deprecation Emails

- **file**: package-lock.json
- **line**: 3227, 4462, 6859
- **category**: hardcoded-pii
- **severity**: low
- **detail**: npm package deprecation messages contain the maintainer email `i@izs.me` (Isaac Z. Schlueter, npm creator). This is auto-generated by npm and present in every Node.js project using these packages.
- **recommendation**: No action needed. This is third-party metadata, not project PII. Cannot and should not be modified.

---

## Categories with No Findings

| Category | Status | Notes |
|---|---|---|
| **Hardcoded PII patterns** | Clean | No SSNs, credit card numbers, phone numbers, IP identifiers, street addresses, or structured personal data found in source files |
| **PII in logs/errors** | Clean | Console statements log only generic status messages (model names, connection status, feature flags). No user PII interpolated |
| **ENV & secret files** | Clean | No `.env` files exist. No `config.yaml`, `docker-compose`, or CI workflow files present. `dotenv.config()` is called in `main.js` but no `.env` file is shipped |
| **Unencrypted storage** | Clean | No password fields, SSN columns, or credit card storage. User data (sessions, settings, memories) is stored as JSON in Electron's `userData` directory — local-only by design |
| **Gitignore gaps** | Clean | `.gitignore` covers `.env*`, `*.pem`, `*.key`, `credentials.*`, `secrets.*`, `*.sql`, `*.csv`, `*.xlsx`. No tracked files match sensitive patterns |

---

## Summary

| Severity | Count |
|---|---|
| Critical | 0 |
| High | 0 |
| Medium | 0 |
| Low | 2 |
| **Total** | **2** |

Both low-severity findings are intentional public disclosures (developer contact email) and third-party metadata (npm deprecation messages). Neither represents an actual PII exposure risk.

**Assessment**: The Aria codebase has an excellent PII posture. The 100% local-only architecture means no user data ever leaves the device — there are no API keys, no cloud endpoints, no telemetry, and no user-facing PII stored in the codebase. The `.gitignore` is comprehensive and covers all standard sensitive file patterns.
